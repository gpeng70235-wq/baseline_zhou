# GitHub delivery plan

Delivery uses `https://github.com/gpeng70235-wq/baseline_zhou.git` on `ipmsm-validation-iter01`, created from current `origin/main`. No force push, history reset, branch deletion, or token-in-URL is permitted. Only the new project is staged.

Before commit: verify reports, all summaries, 20 plots, diagnostics, source manifest, single formal run id, secrets scan, and the repository inventory. Files above 50 MB are excluded and recorded. The content commit message is `Add comprehensive Zhou ICF-MPC IPMSM validation iter01`; the real push result is written to `GITHUB_DELIVERY_REPORT.md`.
